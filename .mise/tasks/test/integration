#!/usr/bin/env bash
#MISE description="Run full-stack integration tests"
set -euo pipefail

# shellcheck source=/dev/null
source "$MISE_PROJECT_ROOT/.venv/bin/activate"
cd "$MISE_PROJECT_ROOT/ansible/tests/integration"
molecule test
